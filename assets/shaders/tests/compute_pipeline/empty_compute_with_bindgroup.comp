#version 450

layout (local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(std430, set = 0, binding = 0) coherent buffer Data
{
    int entries[];
}
data;

void main()
{
    data.entries[gl_GlobalInvocationID.x] = 0;
}
